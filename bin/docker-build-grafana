#!/bin/bash

set -eu

if [ $# -ne 0 ]; then
    echo "no arguments allowed for $(basename $0), given: $@" >&2
    exit 64
fi

bindir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
rootdir="$( cd $bindir/.. && pwd )"

. $bindir/_docker.sh
. $bindir/_tag.sh

dockerfile=$rootdir/grafana/Dockerfile

# IMPORTANT: always try to pull the image with correct arch
extra="--platform ${DOCKER_BUILD_ARCH:-amd64} --pull"
docker_build grafana "$(head_root_tag)" $dockerfile $extra
