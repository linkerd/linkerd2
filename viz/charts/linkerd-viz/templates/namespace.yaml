{{- if (.Values.installNamespace) -}}
---
###
### Linkerd Viz Extension Namespace
###
kind: Namespace
apiVersion: v1
metadata:
  name: {{.Values.namespace}}
  labels:
    {{.Values.extensionLabel}}: linkerd-viz
  annotations:
    {{.Values.proxyInjectAnnotation}}: enabled
{{ end -}}
