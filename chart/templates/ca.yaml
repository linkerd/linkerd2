{{with .Values -}}
---
###
### Control Plane CA Trust
###
---
kind: Secret
apiVersion: v1
metadata:
  name: linkerd-ca
  namespace: {{ .Namespace }}
  labels:
    {{ .ControllerComponentLabel }}: controller
  annotations:
    {{ .CreatedByAnnotation }}: {{ .CliVersion }}
type: Opaque
data:
  crt.pem: {{ b64enc .CATrust.CrtPEM }}
  key.pem: {{ b64enc .CATrust.KeyPEM }}
{{end -}}
